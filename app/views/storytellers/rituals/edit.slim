.data-entry-form
  .breadcrumbs
    = render_breadcrumbs
  h2
    = "Edit Ritual: #{@ritual.name}"

  = form_for @ritual, url: {action: 'update'}, method: 'PATCH' do |f|
    = f.hidden_field :id
    = render partial: 'form', locals: {f: f}
