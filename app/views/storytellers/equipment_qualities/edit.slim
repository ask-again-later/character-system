.data-entry-form
  .breadcrumbs
    = render_breadcrumbs
  h2
    = "Edit Equipment Quality: #{@quality.name}"

  = form_for @quality, url: {action: 'update'}, method: 'PATCH' do |f|
    = f.hidden_field :id
    = render partial: 'form', locals: {f: f}
